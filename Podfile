# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'The social network app' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for The social network app
    pod 'GoogleSignIn'
    pod 'FirebaseAuth'
    pod 'Kingfisher', '~> 7.0'
end
